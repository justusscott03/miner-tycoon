import { background } from "../PJS/colors.js";
import { get } from "../PJS/other.js";

export const images = {
    elevator : function () {

        background(0, 0, 0, 0);

        return get(0, 0, 110, 170);

    }
};
